dependencies:
  override:
    # './...' is a relative pattern which means all subdirectories
   - go build ./...

test:
  override:
    # './...' is a relative pattern which means all subdirectories
    -  go test -v ./...
